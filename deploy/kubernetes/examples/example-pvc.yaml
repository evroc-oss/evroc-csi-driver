---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evroc-test-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: evroc-standard
  resources:
    requests:
      storage: 10Gi
